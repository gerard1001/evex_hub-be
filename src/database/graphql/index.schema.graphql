scalar JSON

type TResponse {
  statusCode: Int!
  message: String!
  data: JSON
}

type Query {
  welcomeMsg: String
}

type IResponse {
  statusCode: Int!
  message: String!
  error: JSON
  data: JSON
}
